Django>=4.2,<5.0
django-allauth[socialaccount]>=0.57.0
stripe>=7.0.0
whitenoise>=6.6.0
python-dotenv>=1.0.0
Pillow>=10.0.0
# PDF extraction (at least one required)
PyMuPDF>=1.23.0        # preferred — best outline/heading extraction
pypdf>=4.0.0           # fallback
# Production
gunicorn>=21.2.0
